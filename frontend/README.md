# Agentic Purchase — Chat UI (Voice + Image)
Chat-like UI with voice and camera that calls FastAPI `/intent/prompt` and `/saga/start` and links to a mock product page.
Run:
  npm install
  # PowerShell: $env:VITE_API_BASE="http://127.0.0.1:8000"
  # Bash/Zsh: export VITE_API_BASE="http://127.0.0.1:8000"
  npm run dev
Open http://127.0.0.1:5174
