# ==== Runtime configuration for Agentic_AI ====
# Copy to `.env` and fill in the values that apply to your environment.

# --- Core Vision setup (required for Agent 1) ---
# Path to the Google service-account credential file for Cloud Vision.
GOOGLE_APPLICATION_CREDENTIALS=C:\Project\Agentic_AI\service-account.json

# --- LLM provider selection ---
# If you want Gemini support, provide the Google API key. Otherwise leave blank.
GOOGLE_API_KEY=

# Default provider is OpenAI; set your key below.
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional LangChain flags (uncomment as needed)
# LANGCHAIN_PROVIDER=openai
# LANGCHAIN_MODEL=gpt-4o-mini
# USE_LANGCHAIN=1
# USE_LANGCHAIN_VISION=1
# USE_LANGCHAIN_INTENT=1
# USE_LANGCHAIN_SOURCING=1
# USE_LANGCHAIN_TRUST=1

# Coordinator settings
# LOG_LEVEL=INFO
# METRICS_ENABLED=0
